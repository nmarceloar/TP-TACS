/**
 * 
 */

package api.rest.views;

// en este package deberiamos tener todas las "vistas" que enviamos al front.
// esto es lo que faltaria.
// por ahora manda el modelo del dominio directamente(en la mayoria de los
// casos, algunos si son vistas).

